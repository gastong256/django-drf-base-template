apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: __PROJECT_SLUG__-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: __PROJECT_SLUG__-web
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
