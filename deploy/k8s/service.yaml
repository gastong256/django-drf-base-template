apiVersion: v1
kind: Service
metadata:
  name: __PROJECT_SLUG__-web
  labels:
    app.kubernetes.io/name: __PROJECT_SLUG__
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: __PROJECT_SLUG__
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: __PORT__
